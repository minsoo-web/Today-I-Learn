기본 문법 
조건문 ? 참일 때 : 거짓일 때 

const array = [1,2,];
let text = array.length === 0
 ? '배열이 비어있습니다.'
 : ' 배열이 비어있지 않습니다.';
 console.log(text);

삼항 연산자는 필수가 아닌 선택(가독성을 높여주기 위해)
중복된 조건문에서도 사용은 가능하지만 가독성이 오히려 떨어지기 때문에 
중복 조건문에서는 if else문이 더 좋다 