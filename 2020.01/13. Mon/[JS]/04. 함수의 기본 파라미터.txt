함수를 호출하게 될 때 
넣어야 할 파라미터를 넣지 않았을 때 
어떤 기본 값을 출력하고 싶으면 ? 

단축 평가 논리 계산 법을 사용하면 편리함 
여기서 ES6 에서 도입된 또 다른 방법이 있다. 

function cal(r=2){
    // 여기서 r=2 를 대입한다는 의미가 아니라 
    // 파라미터를 받지 못할 경우 r=2의 기본 값을 사용한다는 의미 
}

화살표 함수에서도 마찬가지 

const cal = (r=2) => {...}